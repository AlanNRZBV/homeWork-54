import React, { FC } from 'react';
import { ICell } from '../../types';

const Cell: FC<ICell> = ({hasItem, clicked}) => {

  return (
    <div className="cell">

    </div>
  );
};

export default Cell;